#mysql -V   -проаерка бд
Решение 1
Наиболее очевидное решение - убедиться, что мы передаем правильные учетные данные при подключении к MySQL. В первом случае, приведенном выше, я просто ввел неправильный пароль.

Вот пример предоставления правильного пароля:

mysql -uhomer -pWheelyStrongPwd
Результат:

Добро пожаловать в MySQL monitor. Команды заканчиваются на ; или \g.
Ваш идентификатор подключения к MySQL равен 80
Версия сервера: 8.0.29 Homebrew

Авторское право (c) 2000, 2022, Oracle и / или ее филиалы.

Oracle является зарегистрированным товарным знаком корпорации Oracle и / или ее 
филиалов. Другие названия могут быть товарными знаками их соответствующих 
владельцев.

Введите 'help;' или '\h' для получения справки. Введите '\c', чтобы очистить текущую инструкцию ввода.
На этот раз я смог успешно войти в систему, используя homer учетную запись.

Решение 2
Если вы получаете ошибку при попытке что-то сделать с помощью root пользователя, возможно, у root пользователя еще не был установлен пароль.

В этом случае установите пароль для root пользователя:

ALTER USER 'root'@'localhost' IDENTIFIED BY 'Meduza80_';
Измените WheelyStrongPwd пароль на действительно строковый по вашему выбору.

Теперь, когда вы пытаетесь войти в систему как root пользователь, используйте пароль, который вы установили здесь.






Шаг 1. Большинство CMS состоит из файлов и базы данных. С помощью утилиты rsync мы перенесем файлы, а с помощью утилиты mysqldump – перенесем базу.
Начнем с переноса файлов: для этого запустите rsync для корневой директории сайта с сервера-источника на сервер-получатель.
[root@server1 ~]# rsync -avz корневая _директория_сайта/
root@ip_сервера_получателя:/home/bitrix/www/

После переноса всегда можно досинхронизировать файлы, запустив эту команду снова. Копируются только изменения.

Шаг 2. Затем сделаем дамп базы с помощью утилиты mysqldump:

[root@server1 ~]# mysqldump -u имя_пользователя -p ИМЯ_БАЗЫ > ИМЯ_БАЗЫ.sql

И скопируем дамп через тот же rsync

[root@server1 ~]# rsync -avz ИМЯ_БАЗЫ.sql root@ip_сервера_получателя:/home/bitrix/

Примечание
Не делайте и не копируйте дамп базы.sql в корневую директорию сайта, так как к ней могут получить доступ другие пользователи.
 

